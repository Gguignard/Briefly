# ============================================
# Briefly - Variables d'environnement
# Copier ce fichier en .env.local pour le dev
# ============================================

# --- Supabase (Story 1.2) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# --- Clerk Auth (Story 3.1) ---
# Clerk Dashboard > API Keys
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...
# Clerk Dashboard > Webhooks > Add Endpoint
CLERK_WEBHOOK_SECRET=whsec_...

# Clerk Redirect URLs (locale prefix handled dynamically in components)
# These are fallback values - actual URLs use /{locale}/path pattern
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/fr/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/fr/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/fr/summaries
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/fr/summaries

# --- LLM (Story 5.x) ---
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# --- Email Ingestion / Webhooks (Story 4.x) ---
# Cloudflare Email Routing → webhook (pas d'API key requise, config dans Cloudflare Dashboard)
CLOUDFLARE_EMAIL_WEBHOOK_SECRET=
STRIPE_WEBHOOK_SECRET=

# --- BullMQ / Redis (Story 4.4) ---
REDIS_URL=

# --- Monitoring / Logs (Story 1.6) ---
NEXT_PUBLIC_SENTRY_DSN=
# SENTRY_AUTH_TOKEN est REQUIS en CI pour l'upload des source maps
# Générer via Sentry > Settings > Auth Tokens (scope: project:releases)
SENTRY_AUTH_TOKEN=
SENTRY_ORG=briefly
SENTRY_PROJECT=briefly-nextjs

# --- App ---
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development
